import React, {useEffect, useState} from 'react';
import './index.css';

const Waitlist = () => { 
  return (
    <div  className={'align-col-middle view-terms'} style={{height: '55vh'}}>
      <h1>Waitlist</h1>
    </div>
  );
};

export default Waitlist;
